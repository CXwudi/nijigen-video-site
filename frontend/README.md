# 二次元 Video Site - Frontend

The frontend of nijigen-video-site.

## Workspace

The pnpm workspace is rooted in this directory. Use mise to run pnpm from the workspace root:

```sh
mise //frontend:pnpm install --frozen-lockfile
mise //frontend:pnpm list --depth 0
```

Application-specific commands will be added as workspace packages are created.
