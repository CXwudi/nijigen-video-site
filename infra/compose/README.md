# Docker Compose

This folder contains the Docker Compose setup for the whole project, including
both frontend and backend, including dev, CI and prod environments, with a
shared consistent environment via [`common-services.yml`](common-services.yml).
